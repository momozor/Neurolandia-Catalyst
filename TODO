* Make an exception class for CLI::Testing->is_test_path_correct and CLI->carton_execute
* Make migrate helper script to be able to accept db configs as cli parameters
instead of using global $ENV{}.
* Finish authentication operations.
* Settle front-end design (uh,..urgh)
* include an 'overwrite' or 'override' option to migration helper script
* fix Controller::Post edit operation to make user id passing as automatic